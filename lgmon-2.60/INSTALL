To build and install lgmon:

        ./autogen.sh --program-suffix=<Printer Model Name>
        make
        make install

To build and install lgmon with AUTOCONF

You can set the following <Printer Model Name>.
	ip2200
	ip4200
	ip6600d
	ip7500
	mp500

Example for building the ip4200 lgmon with the new automake and autoconf:
        ./autogen.sh --program-suffix=ip4200
        make
        make install

